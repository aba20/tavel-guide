import React from 'react';


const City = props => {
        <div className='column' key={index}>
                    <div>
                    <h1>{item.name}</h1>
                    <h5>France</h5>
                    </div>
                    <p>{item.snippet}</p>
                </div>
    return {cityLoop}
}

export default City;